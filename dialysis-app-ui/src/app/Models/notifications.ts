
interface AlertsForCriticalIssues {
  suddenWeightGain: boolean;
  abnormalBloodPressure: boolean;
  lowProteinIntake: boolean;
}

// TODO: this can be a feature work
interface SimpleCommunicationLogging {
  caregiverNotes?: string;
}